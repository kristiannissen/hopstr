---
import Skeleton from "../../layouts/Skeleton.astro";

import BrewCard from "../../components/BrewCard.astro";

const brews = (await Astro.glob("../../content/brews/*.mdx")) || [];

brews.sort(
  (a, b) =>
    Date.parse(b.frontmatter.published) - Date.parse(a.frontmatter.published),
);

const title = "Bryg arkiv";
---

<Skeleton title={title}>
  <div class="md:max-w-3xl p-4 grid md:grid-cols-2 md:gap-2">
    {
      brews.map((brew) => (
        <article class="min-h-fit min-w-fit mb-8">
          <BrewCard brew={brew} image={false} />
        </article>
      ))
    }
  </div>
</Skeleton>
